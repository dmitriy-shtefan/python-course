**Вправа 9. Збери програму**

Інструкція: Рядки коду надані в неправильному порядку. Потрібно скласти працюючу програму.

Перемішані рядки:

```python
print("Вік у місяцях:", months)
age = int(input("Введи вік: "))
months = age * 12
print("Старт програми")
```

Приклад вводу/виводу:

```text
Ввід:
18

Вивід:
Старт програми
Вік у місяцях: 216
```